# Presentation templates

Template definitions are database records with safe layout metadata. Phase 1 uses the branded server renderer in `backend/src/presentations/presentations.service.ts`; this directory is reserved for approved logo/font assets and future signed template packages.

'use client';import Link from 'next/link';import {FormEvent,useState} from 'react';import {api} from '@/lib/api';
export default function ForgotPassword(){const [busy,setBusy]=useState(false);const [done,setDone]=useState(false);const [error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const form=new FormData(e.currentTarget);try{await api('/auth/forgot-password',{method:'POST',body:JSON.stringify({email:form.get('email')})});setDone(true)}catch(err){setError(err instanceof Error?err.message:'অনুরোধ পাঠানো যায়নি')}finally{setBusy(false)}}return <main className="auth-page"><form className="auth-card card" onSubmit={submit}><div className="seal auth-seal">NHA</div><div className="eyebrow">অ্যাকাউন্ট পুনরুদ্ধার</div><h1>পাসওয়ার্ড পুনঃনির্ধারণ</h1><p className="subtle">আপনার অফিসিয়াল ইমেইল দিন। অ্যাকাউন্টটি থাকলে পুনরুদ্ধারের নির্দেশনা পাঠানো হবে।</p>{done?<><p className="success">অনুরোধ গ্রহণ করা হয়েছে। অনুমোদিত যোগাযোগমাধ্যম দেখুন।</p><Link className="btn btn-primary full" href="/login">লগইনে ফিরুন</Link></>:<><div className="field"><label htmlFor="email">ইমেইল ঠিকানা</label><input className="input" id="email" name="email" type="email" required/></div>{error?<p className="error">{error}</p>:null}<button className="btn btn-primary full" disabled={busy}>{busy?'পাঠানো হচ্ছে…':'নির্দেশনা পাঠান'}</button><Link className="auth-back" href="/login">← লগইনে ফিরুন</Link></>}</form></main>}

'use client';
import Link from 'next/link';import {useCallback,useState} from 'react';import AppShell from '@/components/AppShell';import {ErrorState,LoadingState} from '@/components/PageState';import StatusBadge from '@/components/StatusBadge';import {api,formatBytes,formatDate} from '@/lib/api';import {useLoadEffect as useEffect} from '@/hooks/useLoadEffect';
type Dashboard={totalPhotos:number;projects:number;events:number;pendingReviews:number;photosUploadedToday:number;photosUploadedThisMonth:number;storageUsedBytes:number;recentUploads:{id:string;title:string;status:string;createdAt:string}[];monthlyStorageGrowth:{primaryBytes:string;recordedAt:string}[]};
export default function DashboardPage(){const [data,setData]=useState<Dashboard|null>(null);const [error,setError]=useState('');const load=useCallback(()=>api<Dashboard>('/dashboard').then(setData).catch((e:Error)=>setError(e.message)),[]);useEffect(load,[load]);const growth=[...(data?.monthlyStorageGrowth??[])].reverse();const max=Math.max(...growth.map(x=>Number(x.primaryBytes)),1);return <AppShell title="ড্যাশবোর্ড"><div className="content"><div className="pagehead"><div><div className="eyebrow">কার্যক্রমের সারসংক্ষেপ</div><h1>আর্কাইভ ড্যাশবোর্ড</h1><p>বর্তমান প্রাতিষ্ঠানিক ছবি সংগ্রহ ও অনুমোদন কার্যক্রমের অবস্থা।</p></div><div className="actions"><Link className="btn btn-secondary" href="/photos?status=PENDING_REVIEW">অনুমোদনের তালিকা</Link><Link className="btn btn-primary" href="/photos/upload">ছবি আপলোড</Link></div></div>{error?<ErrorState message={error} retry={load}/>:null}{!data&&!error?<LoadingState label="আর্কাইভের হিসাব লোড হচ্ছে…"/>:data?<><section className="stats"><div className="card stat"><div className="stat-label">মোট ছবি</div><div className="stat-value">{data.totalPhotos.toLocaleString('bn-BD')}</div><div className="stat-note">এই মাসে {data.photosUploadedThisMonth.toLocaleString('bn-BD')}টি যোগ হয়েছে</div></div><div className="card stat"><div className="stat-label">প্রকল্প</div><div className="stat-value">{data.projects.toLocaleString('bn-BD')}</div><div className="stat-note">নিবন্ধিত উদ্যোগ</div></div><div className="card stat"><div className="stat-label">দাপ্তরিক কার্যক্রম</div><div className="stat-value">{data.events.toLocaleString('bn-BD')}</div><div className="stat-note">আজ {data.photosUploadedToday.toLocaleString('bn-BD')}টি আপলোড</div></div><div className="card stat stat-attention"><div className="stat-label">অনুমোদনের অপেক্ষায়</div><div className="stat-value">{data.pendingReviews.toLocaleString('bn-BD')}</div><Link href="/photos?status=PENDING_REVIEW">তালিকা খুলুন →</Link></div></section><section className="dashboard-grid"><div className="card section"><div className="section-heading"><div><h3>সাম্প্রতিক আপলোড</h3><p>আর্কাইভে সর্বশেষ সংযোজন</p></div><Link href="/photos">সব দেখুন</Link></div>{data.recentUploads.length?data.recentUploads.map(item=><Link className="activity" key={item.id} href={`/photos/${item.id}`}><span className="activity-dot"/><div className="activity-body"><p>{item.title}</p><time>{formatDate(item.createdAt,true)}</time></div><StatusBadge value={item.status}/></Link>):<div className="empty">এখনও কোনো ছবি আপলোড হয়নি।</div>}</div><div className="card section"><div className="section-heading"><div><h3>স্টোরেজ ব্যবহার</h3><p>অপ্টিমাইজ করা মূল মিডিয়া</p></div><strong>{formatBytes(data.storageUsedBytes)}</strong></div><div className="growth-chart" aria-label="মাসিক স্টোরেজ বৃদ্ধি">{growth.length?growth.map((point,index)=><div className="growth-column" key={`${point.recordedAt}-${index}`}><span style={{height:`${Math.max(8,Number(point.primaryBytes)/max*100)}%`}}/><small>{new Date(point.recordedAt).toLocaleDateString('bn-BD',{month:'short'})}</small></div>):<div className="chart-empty">পরবর্তী স্টোরেজ নমুনা থেকে বৃদ্ধির ইতিহাস দেখা যাবে।</div>}</div><div className="storage-callout"><span className="health-dot"/>মূল স্টোরেজের হিসাব ব্যাকআপ থেকে আলাদা।</div></div></section><section className="quick-links"><Link className="card quick-card" href="/projects"><span>০১</span><div><strong>প্রকল্প আর্কাইভ</strong><small>উন্নয়ন উদ্যোগগুলো দেখুন</small></div>→</Link><Link className="card quick-card" href="/events"><span>০২</span><div><strong>কার্যক্রম নিবন্ধন</strong><small>দাপ্তরিক কার্যক্রমের নথি দেখুন</small></div>→</Link><Link className="card quick-card" href="/presentations/create"><span>০৩</span><div><strong>উপস্থাপনা তৈরি</strong><small>এনএইচএ ব্রিফিং প্রস্তুত করুন</small></div>→</Link></section></>:null}</div></AppShell>}

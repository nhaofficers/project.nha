'use client';import Link from 'next/link';import {FormEvent,Suspense,useState} from 'react';import {useSearchParams} from 'next/navigation';import {api} from '@/lib/api';
function Form(){const params=useSearchParams();const [busy,setBusy]=useState(false);const [done,setDone]=useState(false);const [error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const password=String(f.get('password'));if(password!==f.get('confirm')){setError('পাসওয়ার্ড দুটি মিলছে না।');return}setBusy(true);try{await api('/auth/reset-password',{method:'POST',body:JSON.stringify({token:params.get('token')??'',password})});setDone(true)}catch(err){setError(err instanceof Error?err.message:'পাসওয়ার্ড পরিবর্তন করা যায়নি')}finally{setBusy(false)}}return <form className="auth-card card" onSubmit={submit}><div className="seal auth-seal">NHA</div><h1>নতুন পাসওয়ার্ড দিন</h1><p className="subtle">কমপক্ষে ১২ অক্ষরের একটি শক্তিশালী পাসওয়ার্ড ব্যবহার করুন।</p>{done?<><p className="success">পাসওয়ার্ড সফলভাবে পরিবর্তিত হয়েছে।</p><Link className="btn btn-primary full" href="/login">লগইন করুন</Link></>:<><div className="field"><label htmlFor="password">নতুন পাসওয়ার্ড</label><input className="input" id="password" name="password" type="password" minLength={12} required/></div><div className="field"><label htmlFor="confirm">পাসওয়ার্ড নিশ্চিত করুন</label><input className="input" id="confirm" name="confirm" type="password" minLength={12} required/></div>{error?<p className="error">{error}</p>:null}<button className="btn btn-primary full" disabled={busy}>{busy?'পরিবর্তন হচ্ছে…':'পাসওয়ার্ড পরিবর্তন'}</button></>}</form>}
export default function ResetPassword(){return <main className="auth-page"><Suspense fallback={<div>লোড হচ্ছে…</div>}><Form/></Suspense></main>}
